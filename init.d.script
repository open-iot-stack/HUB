#!/bin/bash
#/etc/init.d/stratusprint-printer

case "$1" in
    start)
        echo "Starting stratusprint-printer connection daemon"
        $COMMAND$
        ;;
    stop)
        echo "Stopping stratusprint-printer connection daemon"
        killproc $prog -TERM
        ;;
esac

exit 0
